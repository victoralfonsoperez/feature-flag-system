# Uncomment to prevent lifecycle scripts (postinstall, prepare, etc.) from running during install.
# Useful in CI or security-sensitive environments where you don't want third-party scripts to execute.
# ignore-scripts=true
