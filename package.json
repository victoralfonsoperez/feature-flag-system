{
  "name": "feature-flag-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/api & npm run dev -w packages/dashboard & wait",
    "build": "npm run build --workspaces --if-present",
    "test": "docker compose -f docker-compose.test.yml up -d --wait && npm run test --workspaces --if-present ; docker compose -f docker-compose.test.yml down",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "globals": "^17.4.0",
    "typescript-eslint": "^8.56.1"
  }
}
